services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/checks.db
      - ALERT_WEBHOOK_URL=${ALERT_WEBHOOK_URL}
      - CHECK_TIMEOUT_MS=5000
    restart: unless-stopped
